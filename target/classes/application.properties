spring.jpa.database=postgresql
spring.datasource.url=jdbc:postgresql://localhost:5432/cliente?useSSL=false
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
#spring.jackson.date-format=yyyy-MM-dd

spring.jackson.deserialization.fail-on-unknown-properties=true


#----------------------------------------------------------------------------------------
#Para n�o mostrar a exception do postgres no start (Metodo n�o implementado)
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false

#N�o mostrar erro ao (buscar por id), (deletar(depende do buscar por id))...
spring.jackson.serialization.fail-on-empty-beans=false

#----------------------------------------------------------------------------------------
#Desnecessario
#spring.jpa.hibernate.ddl-auto = validate
#Desnecessario
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
#Desnecessario
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect